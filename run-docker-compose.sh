#!/bin/bash

# Run with Docker Compose
docker-compose up -d

echo "Docker Compose has started the 'checklist-app' container."
echo "Access the application at http://localhost:3005"
echo "To stop the container, use: docker-compose down"